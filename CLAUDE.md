# Project Rules

Make sure to follow these rules unless explicitly instructed otherwise.

## Dart/Flutter

- Flutter SDK is managed by FVM. Always run flutter/dart commands via fvm.
- This project uses Pub Workspaces. Always run static analysis from the project
  root instead of subdirectories such as `./client`.
