import 'package:flutter/widgets.dart';

// Design tokens: colors. Defined as top-level constants (not a static-only
// class) to satisfy avoid_classes_with_only_static_members.
const colorBackground = Color(0xFFFFFFFF);
const colorSurface = Color(0xFFF5F5F5);
const colorText = Color(0xFF1A1A1A);
const colorTextMuted = Color(0xFF6B6B6B);
const colorAccent = Color(0xFF1565C0);
const colorError = Color(0xFFB00020);
const colorDivider = Color(0xFFE0E0E0);
