targets:
  $default:
    builders:
      json_serializable:
        options:
          # API JSON is snake_case; Dart models are camelCase.
          field_rename: snake
          explicit_to_json: true
