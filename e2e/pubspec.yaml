name: e2e
publish_to: "none"

environment:
  sdk: ^3.12.2
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  patrol: ^4.7.1
  http: ^1.6.0

patrol:
  app_name: E2E Testing
  test_directory: test
  android:
    package_name: dev.norelease.paperdoll
